/**
 * config.js
 * Why:
 * - For MVP you don't have auth yet, so we use a fixed user
 * - We keep this in ONE file so later we replace it with auth easily
 */
export const USER_ID = "cmklwr4zv00007xuls1gxm7u3";
